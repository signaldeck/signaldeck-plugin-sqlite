# SignalDeck SQLite Plugin

This repository contains the sqlite-datastore for the signaldeck framework.

## Installation

```bash
pip install signaldeck-plugin-hello-world


Entwicklung:
 ```
 py -m pip install -e . --config-settings editable_mode=compat
 ```